# Change 4 行为规格索引

本目录的三个 YAML 文件分别定义 `tenant-resource-isolation`、`tenant-failure-matrix` 和 `v0.3-aggregate-acceptance` 的 Requirements 与 Given/When/Then Scenarios。Canonical agent checkpoints 与 code specs 当前保持一一对应。
