meta:
  task_id: "detector-framework"
  change_id: "2026-08-22-v0.2.0-flow-foundation"
  task_type: code
  system: "z LLM Safety Gateway detector-framework"
  preconditions:
    - "Phase 2 spec locked and the referenced RED test node exists"
steps:
  - id: CP-001
    description: "SC-DF-701 四类 Detector 通过统一 Capability lifecycle 协调器参与 Flow"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/detectors/test_flow_adapter_lifecycle.py::test_tc_df_701"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-002
    description: "SC-DF-702 四类 Detector 通过统一 Capability lifecycle 协调器参与 Flow"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/detectors/test_flow_adapter_lifecycle.py::test_tc_df_702"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-003
    description: "SC-DF-703 Detector SDK 与插件发现契约保持不变"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/detectors/test_flow_adapter_lifecycle.py::test_tc_df_703"
    assertions:
      - type: exit_code
        expected: 0
rollback:
  steps:
    - "Revert only the implementation slice associated with the failing checkpoint; preserve RED test evidence"
