meta:
  task_id: "degraded-safety-visibility"
  change_id: "2026-08-22-v0.2.0-flow-foundation"
  task_type: code
  system: "z LLM Safety Gateway degraded-safety-visibility"
  preconditions:
    - "Phase 2 spec locked and the referenced RED test node exists"
steps:
  - id: CP-001
    description: "SC-DSV-701 请求级 Flow snapshot 在全部阶段一致暴露 fail-open 降级"
    action: "cd ../.. && .venv/bin/pytest -q tests/integration/test_flow_degradation.py::test_tc_dsv_701"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-002
    description: "SC-DSV-702 请求级 Flow snapshot 在全部阶段一致暴露 fail-open 降级"
    action: "cd ../.. && .venv/bin/pytest -q tests/integration/test_flow_degradation.py::test_tc_dsv_702"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-003
    description: "SC-DSV-703 每种降级原因和适用策略均可解释且不泄密"
    action: "cd ../.. && .venv/bin/pytest -q tests/integration/test_flow_degradation.py::test_tc_dsv_703"
    assertions:
      - type: exit_code
        expected: 0
rollback:
  steps:
    - "Revert only the implementation slice associated with the failing checkpoint; preserve RED test evidence"
