meta:
  task_id: "config-system"
  change_id: "2026-08-22-v0.2.0-flow-foundation"
  task_type: code
  system: "z LLM Safety Gateway config-system"
  preconditions:
    - "Phase 2 spec locked and the referenced RED test node exists"
steps:
  - id: CP-001
    description: "SC-CFG-001 配置系统解析严格的新 Flow schema 与 stage 引用"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_001"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-002
    description: "SC-CFG-002 配置系统解析严格的新 Flow schema 与 stage 引用"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_002"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-003
    description: "SC-CFG-003 旧 YAML 保持加载并编译为等价 Flow"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_003"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-004
    description: "SC-CFG-004 旧 YAML 保持加载并编译为等价 Flow"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_004"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-005
    description: "SC-CFG-005 跨 Flow 验证在启动阶段拒绝不安全或不确定配置"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_005"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-006
    description: "SC-CFG-006 安全配置文档示例由真实运行时模型反向验证"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/config/test_flow_config.py::test_tc_cfg_006"
    assertions:
      - type: exit_code
        expected: 0
rollback:
  steps:
    - "Revert only the implementation slice associated with the failing checkpoint; preserve RED test evidence"
