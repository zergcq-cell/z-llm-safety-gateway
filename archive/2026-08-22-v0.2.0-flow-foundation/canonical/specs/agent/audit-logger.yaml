meta:
  task_id: "audit-logger"
  change_id: "2026-08-22-v0.2.0-flow-foundation"
  task_type: code
  system: "z LLM Safety Gateway audit-logger"
  preconditions:
    - "Phase 2 spec locked and the referenced RED test node exists"
steps:
  - id: CP-001
    description: "SC-AUD-701 审计记录以加法字段关联 Flow 与 Node 证据"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/audit/test_flow_evidence.py::test_tc_aud_701"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-002
    description: "SC-AUD-702 审计记录以加法字段关联 Flow 与 Node 证据"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/audit/test_flow_evidence.py::test_tc_aud_702"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-003
    description: "SC-AUD-703 审计证据与内容存储策略分离并默认保护数据"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/audit/test_flow_evidence.py::test_tc_aud_703"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-004
    description: "SC-AUD-704 流式证据和 sink 故障采用显式有界持久化"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/audit/test_flow_evidence.py::test_tc_aud_704"
    assertions:
      - type: exit_code
        expected: 0
  - id: CP-005
    description: "SC-AUD-705 流式证据和 sink 故障采用显式有界持久化"
    action: "cd ../.. && .venv/bin/pytest -q tests/unit/audit/test_flow_evidence.py::test_tc_aud_705"
    assertions:
      - type: exit_code
        expected: 0
rollback:
  steps:
    - "Revert only the implementation slice associated with the failing checkpoint; preserve RED test evidence"
