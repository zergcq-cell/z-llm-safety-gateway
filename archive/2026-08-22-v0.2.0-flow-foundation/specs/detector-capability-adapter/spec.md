# Capability: detector-capability-adapter

## ADDED / MODIFIED Requirements

- **REQ-DCA-001**：Detector 被映射为版本化 Capability descriptor（1 scenarios）
- **REQ-DCA-002**：Adapter 精确保留 Detector 输入、结果和修改语义（2 scenarios）
- **REQ-DCA-003**：Adapter 复用既有 lifecycle/status 且正确传播错误与取消（2 scenarios）

## Verification

- Canonical spec: `canonical/specs/code/detector-capability-adapter.yaml`
- Agent checkpoints: `canonical/specs/agent/detector-capability-adapter.yaml`
- Test cases: 5
