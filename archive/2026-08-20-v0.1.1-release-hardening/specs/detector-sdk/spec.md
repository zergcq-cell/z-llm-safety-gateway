# Capability: detector-sdk

## MODIFIED Requirements

- **REQ-SDK-007**：SDK SHALL 通过 Ruff/Mypy/tests，CLI 返回类型明确，版本元数据 SHALL 一致为 0.1.1。
- **REQ-SDK-008**：SDK wheel SHALL 能在无 gateway 包的干净环境安装并运行 `zlg-sdk --help`。
