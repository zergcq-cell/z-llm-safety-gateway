meta:
  task_id: "performance-benchmark"
  change_id: "2026-08-20-v0.1.1-release-hardening"
  task_type: code
  system: "发布性能基准与报告"
  preconditions: ["规则 detector mix 固定"]
steps:
  - {id: CP-BENCH-004, description: "单 suite 真实性", action: ".venv/bin/pytest tests/unit/release/test_benchmark_contract.py -q", assertions: [{type: exit_code, expected: 0}]}
  - {id: CP-BENCH-005, description: "可比较发布报告", action: ".venv/bin/python -m tests.benchmarks.bench_pipeline --suite all --output /tmp/zlg-release-benchmark-verify.md", assertions: [{type: exit_code, expected: 0}]}
rollback:
  steps: ["保留旧基准报告，新报告按日期独立记录"]
